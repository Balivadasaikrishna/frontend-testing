name: Frontend Testing CI/CD Pipeline

on:
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main ]

env:
  NODE_VERSION: '18'
  PLAYWRIGHT_VERSION: '1.54.2'

jobs:
  # Unit Tests Job
  unit-tests:
    name: Unit Tests
    runs-on: ubuntu-latest
    strategy:
      matrix:
        node-version: [18, 20]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js ${{ matrix.node-version }}
      uses: actions/setup-node@v4
      with:
        node-version: ${{ matrix.node-version }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Run unit tests
      run: npm run test:unit
      
    - name: Generate test coverage report
      run: npm run test:coverage

  # E2E Tests Job (Temporarily disabled due to browser installation issues)
  # e2e-tests:
  #   name: E2E Tests
  #   runs-on: ubuntu-latest
  #   
  #   steps:
  #   - name: Checkout code
  #     uses: actions/checkout@v4
  #     
  #   - name: Setup Node.js
  #     uses: actions/setup-node@v4
  #     with:
  #       node-version: ${{ env.NODE_VERSION }}
  #       cache: 'npm'
  #       
  #   - name: Install dependencies
  #     run: npm ci
  #     
  #   - name: Install Playwright browsers
  #     run: |
  #       echo "Installing Playwright browsers..."
  #       npx playwright install --with-deps chromium
  #       echo "Browser installation step 1 complete"
  #       
  #   - name: Force browser reinstall
  #     run: |
  #       echo "Force reinstalling browsers..."
  #       npx playwright install chromium
  #       echo "Force reinstall complete"
  #       
  #   - name: Debug browser installation
  #     run: |
  #       echo "=== Debugging browser installation ==="
  #       echo "Current directory: $(pwd)"
  #       echo "Node modules path: $(ls -la node_modules/ | grep playwright || echo 'No playwright in node_modules')"
  #       echo "Playwright version: $(npx playwright --version || echo 'Playwright not found')"
  #       echo "Browser path check:"
  #       find . -name "chromium*" -type d 2>/dev/null | head -3 || echo "No chromium directories found"
  #       echo "=== Debug complete ==="
  #     
  #   - name: Try alternative browser installation
  #     run: |
  #       echo "Trying alternative browser installation..."
  #       export PLAYWRIGHT_BROWSERS_PATH=0
  #       npx playwright install chromium
  #       echo "Alternative installation complete"
  #     
  #   - name: Run E2E tests
  #     run: npx playwright test --project=chromium --reporter=list
  #     env:
  #       PLAYWRIGHT_BROWSERS_PATH: 0
  #       CI: true
  #     
  #   - name: Upload E2E test results
  #     uses: actions/upload-artifact@v4
  #     if: always()
  #     with:
  #       name: e2e-test-results
  #       path: test-results/
  #       retention-days: 7

  # Build Job
  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: [unit-tests]
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Setup Node.js
      uses: actions/setup-node@v4
      with:
        node-version: ${{ env.NODE_VERSION }}
        cache: 'npm'
        
    - name: Install dependencies
      run: npm ci
      
    - name: Build application
      run: npm run build
      
    - name: List build output
      run: ls -la dist/
      
    - name: Verify build output
      run: |
        if [ ! -f "dist/index.js" ]; then
          echo "Error: dist/index.js not found"
          exit 1
        fi
        if [ ! -f "dist/index.html" ]; then
          echo "Error: dist/index.html not found"
          exit 1
        fi
        echo "Build verification successful"
      
    - name: Upload build artifacts
      uses: actions/upload-artifact@v4
      with:
        name: build-files
        path: dist/
        retention-days: 7
